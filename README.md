# Cración de API REST con Flask

```bash
python3 -m venv venv
source venv/bin/activate
```

```bash
pip install -r requirements.txt
```

o

```bash
pip install Flask flask-cors python-dotenv google-generativeai
```
